# ── Git history ──────────────────────────────────────────────────────
.git/

# ── Large generated artifacts (not needed inside the image) ──────────
# These two files total ~5 MB and are excluded from the Docker build
# context to keep builds fast.  They are regenerated by build_explorer.py
# and served as static assets — *not* used by the FastAPI server.
extracted_data.json
index.html

# ── Build tooling ─────────────────────────────────────────────────────
build_explorer.py

# ── Tests & dev requirements ──────────────────────────────────────────
tests/
pytest.ini
requirements-dev.txt

# ── Python bytecode ───────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# ── Editor / OS artefacts ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
